#!/usr/bin/env ruby
# frozen_string_literal: true

# Extract the artworks carousel from a Google SERP and print it as JSON.
#   bin/extract path/to/serp.html
require_relative "../lib/serpapi_code_challenge"

exit SerpapiCodeChallenge::CLI.run(ARGV)
